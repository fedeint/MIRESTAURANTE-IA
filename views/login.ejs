<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Iniciar Sesion - mirestconia.com</title>
  <link href="/vendor/bootstrap/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="/vendor/bootstrap-icons/bootstrap-icons.css">
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700;800;900&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="/css/theme.css">
</head>
<body style="min-height:100vh; display:flex; flex-direction:column;">
  <main style="flex:1 0 auto; display:flex; align-items:center;">
    <div class="container">
      <div class="row justify-content-center">
        <div class="col-12 col-md-5 col-lg-4">
          <!-- Brand header -->
          <div class="text-center mb-4">
            <div style="width:60px;height:60px;background:linear-gradient(135deg,#818cf8,#06b6d4);border-radius:1rem;display:inline-flex;align-items:center;justify-content:center;font-size:1.5rem;color:#fff;box-shadow:0 8px 24px rgba(99,102,241,0.3);margin-bottom:0.75rem;">
              <i class="bi bi-fire"></i>
            </div>
            <h4 style="font-weight:800;letter-spacing:-0.03em;color:#1e293b;">dignita<span style="color:#06b6d4;">.tech</span></h4>
            <p class="text-muted small">Sistema de Gestion de Restaurantes</p>
          </div>
          <!-- Card -->
          <div class="card">
            <div class="card-body p-4">
              <div class="d-flex align-items-center mb-3">
                <div class="me-2" style="color:#6366f1;"><i class="bi bi-shield-lock-fill fs-3"></i></div>
                <div>
                  <div class="fw-bold">Iniciar sesion</div>
                  <div class="text-muted small">Accede segun tu rol (admin / mesero / cocinero).</div>
                </div>
              </div>

              <% if (error) { %>
                <div class="alert alert-danger py-2 small"><%= error %></div>
              <% } %>

              <form method="POST" action="/login" autocomplete="off">
                <div class="mb-3">
                  <label class="form-label">Usuario</label>
                  <div class="input-group">
                    <span class="input-group-text"><i class="bi bi-person-fill"></i></span>
                    <input name="usuario" class="form-control" required autofocus placeholder="Tu usuario">
                  </div>
                </div>
                <div class="mb-3">
                  <label class="form-label">Contrasena</label>
                  <div class="input-group">
                    <span class="input-group-text"><i class="bi bi-lock-fill"></i></span>
                    <input name="password" type="password" class="form-control" required placeholder="Tu contrasena">
                  </div>
                </div>
                <button class="btn btn-primary w-100 py-2" type="submit">
                  <i class="bi bi-box-arrow-in-right me-1"></i> Entrar
                </button>
              </form>

              <div class="text-muted small mt-3" style="background:rgba(99,102,241,0.04);padding:0.75rem;border-radius:0.625rem;">
                <i class="bi bi-info-circle me-1"></i>
                Primer acceso: <strong>admin</strong> / <strong>admin123</strong>. Cambia la contrasena despues.
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
  </main>

  <%- include('partials/footer') %>

  <script src="/vendor/bootstrap/js/bootstrap.bundle.min.js"></script>
</body>
</html>
